import { SafariDatePipe } from './safari-date.pipe';

describe('SafariDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SafariDatePipe();
    expect(pipe).toBeTruthy();
  });
});
