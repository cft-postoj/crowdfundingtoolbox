import { TransferTypeConvertToStringPipe } from './transfer-type-convert-to-string.pipe';

describe('TransferTypeConvertToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new TransferTypeConvertToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
