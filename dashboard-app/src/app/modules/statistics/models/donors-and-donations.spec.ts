import { DonorsAndDonations } from './donors-and-donations';

describe('DonorsAndDonations', () => {
  it('should create an instance', () => {
    expect(new DonorsAndDonations()).toBeTruthy();
  });
});
