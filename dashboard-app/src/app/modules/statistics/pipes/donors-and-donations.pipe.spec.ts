import { DonorsAndDonationsPipe } from './donors-and-donations.pipe';

describe('DonorsAndDonationsPipe', () => {
  it('create an instance', () => {
    const pipe = new DonorsAndDonationsPipe();
    expect(pipe).toBeTruthy();
  });
});
