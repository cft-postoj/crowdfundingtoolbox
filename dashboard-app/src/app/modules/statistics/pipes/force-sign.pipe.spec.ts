import { ForceSignPipe } from './force-sign.pipe';

describe('ForceSignPipe', () => {
  it('create an instance', () => {
    const pipe = new ForceSignPipe();
    expect(pipe).toBeTruthy();
  });
});
